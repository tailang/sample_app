<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "发表行者话语......" %>
	</div>
	<%= f.submit "发布", class: "btn btn-large btn-success" %>
<% end %>