<% provide(:title, '注册') %>
<h2>注册</h2>
<div class="row">
	<div class="span6 offset3">
		<%= form_for(@user) do |f| %>
			<%= render 'shared/error_messages', object: f.object %>
			<!-- 在上面的代码中渲染的局部图名为shared/error_messages,这里用到了rails的
			一个约定：如果局部试图要在过个控制器中使用，则要把它存放在专门的shared目录下 -->
		<%= f.label "用户名" %>
		<%= f.text_field :name %>
		<small>字母、数字、汉字或'_'</small>
		<br><br>

		<%= f.label "邮箱" %>
		<%= f.text_field :email %>
		<small>请输入你常用的邮箱，一旦注册不可修改</small>
		<br><br>

		<%= f.label "密码" %>
		<%= f.password_field :password %>
		<small>至少输入6个字符</small>
		<br><br>

		<%= f.label :password_confirmation,"确认密码" %>
		<%= f.password_field :password_confirmation %>
		<br><br>

		<%= f.submit "创建用户", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>